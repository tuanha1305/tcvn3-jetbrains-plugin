<idea-plugin>
  <id>io.github.tuanha1305.tcvn3</id>
  <name>TCVN3 Vietnamese Encoding</name>
  <version>1.0.0</version>
  <vendor email="tuancntt971@gmail.com" url="https://github.com/tuanha1305/tcvn3-jetbrains-plugin">tuanha1305</vendor>

  <description><![CDATA[
    Adds the legacy Vietnamese <b>TCVN3</b> (also known as ABC) encoding to all
    JetBrains IDEs. After installing, "TCVN3" appears in the encoding dropdown
    (status bar / File Encoding settings) and works for file open / save / Find
    in Files like any built-in charset.
    <br/><br/>
    Registered via the standard Java SPI
    (<code>java.nio.charset.spi.CharsetProvider</code>) - no IDE files patched,
    no monkey-patching, just a properly-registered <code>java.nio.charset.Charset</code>.
    <br/><br/>
    <b>Round-trip safe</b>: non-TCVN3 bytes (e.g. GBK Chinese filenames inside
    game data files) decode into the Unicode Private Use Area and re-encode
    byte-identical, so saving never corrupts mixed-encoding files.
  ]]></description>

  <change-notes><![CDATA[
    <ul>
      <li>1.0.0 - Initial release. Registers the TCVN3 charset for the entire
          IntelliJ Platform via Java SPI. Mirrors the codec used by
          <a href="https://github.com/tuanha1305/vs-code-tcvn3-customize">vs-code-tcvn3-customize</a>.</li>
    </ul>
  ]]></change-notes>

  <!-- Compatible with all IntelliJ-based IDEs. since-build / until-build are
       set via the Gradle plugin's pluginConfiguration block. -->
  <depends>com.intellij.modules.platform</depends>
</idea-plugin>
